#ifndef ARRAY_H_HXD4EXKY
#define ARRAY_H_HXD4EXKY

#include <algorithm>
#include <cmath>
#include <iostream>
#include <vector>

namespace array {
std::vector<int> spiralOrder(const std::vector<std::vector<int>> &v);
int coverPoints(const std::vector<int> &, const std::vector<int> &);
std::vector<int> addOne(const std::vector<int> &);
void run();
}  // namespace array

#endif /* end of include guard: ARRAY_H_HXD4EXKY */
